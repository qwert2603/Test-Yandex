<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="TextSecondary">
        <item name="android:textSize">@dimen/info_text_size</item>
        <item name="android:textColor">@color/info_text_color</item>
    </style>

    <style name="TextSecondary.WithMargins">
        <item name="android:layout_marginTop">@dimen/details_text_view_margin</item>
        <item name="android:layout_marginStart">@dimen/details_text_view_margin</item>
        <item name="android:layout_marginEnd">@dimen/details_text_view_margin</item>
    </style>

    <style name="TextMain">
        <item name="android:textSize">@dimen/name_text_size</item>
        <item name="android:textColor">@color/name_text_color</item>
    </style>

    <style name="TextMain.WithMargins">
        <item name="android:layout_marginTop">@dimen/details_text_view_margin</item>
        <item name="android:layout_marginStart">@dimen/details_text_view_margin</item>
        <item name="android:layout_marginEnd">@dimen/details_text_view_margin</item>
    </style>

</resources>
